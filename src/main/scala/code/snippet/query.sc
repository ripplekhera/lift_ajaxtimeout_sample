"""select success_login,
  |   attempt_login
  |from
  |  (select count(la.session_id) success_login,
  |      date(lf.dt) today
  |   from login_attempt la,
  |    login_fact lf
  |   where la.session_id = lf.session_id
  |    and date(lf.dt) = ?) query1
  |  left join
  |  (select count(la.session_id) attempt_login,
  |      date(la.dt) today
  |   from login_attempt la
  |   where date(la.dt) = ?) query2 on query1.today = query2.today """.stripMargin































































































































































































































































